package LinkedList.easy;

/*
 * 
class GfG{
    long mod = 1000000007;
  
   public long multiplyTwoLists(Node l1,Node l2){
        long num1 = number(l1);
        long num2 = number(l2);
        
        return ((num1 * num2)%mod);
   }
   
   public long number(Node head) {
       long sum = 0;
       while(head != null) {
           int digit = head.data;
           sum = (((sum * 10) % mod) + digit) % mod;
           head = head.next;
       }
       
       return sum;
   }
}
 */